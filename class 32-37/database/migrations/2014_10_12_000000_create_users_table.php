<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('firstName');
            $table->string('lastName');
            $table->string('email')->unique(); //unique(), fara com que tenha apenas 1 email e nenhum outro usuario possa usar
            $table->timestamp('email_verified_at')->nullable(); //nullable, permite valores nulos
            $table->string('password');
            $table->rememberToken(); //Trabalha com recuperação de senha
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('users');
    }
};
